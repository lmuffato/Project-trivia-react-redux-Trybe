import React from 'react';

class Settings extends React.Component {
  render() {
    return (
      <div>
        <h2 data-testid="settings-title">Tela de configuração</h2>
      </div>
    );
  }
}

export default Settings;
