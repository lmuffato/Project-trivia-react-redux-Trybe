import React, { Component } from 'react';

class Configs extends Component {
  render() {
    return (
      <h1 data-testid="settings-title">Tela de configuracoes</h1>
    );
  }
}

export default Configs;
