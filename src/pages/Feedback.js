import React from 'react';
import Header from '../components/gameHeader';

const Feedback = () => (
  <header>
    <h1 data-testid="feedback-text">Tela Feedback</h1>
    <Header />
  </header>
);

export default Feedback;
