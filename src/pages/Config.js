import React, { Component } from 'react';

class Config extends Component {
  render() {
    return (
      <h1 data-testid="settings-title">
        Bem Vindo a tela de configuração não funcional!! :D
      </h1>);
  }
}

export default Config;
