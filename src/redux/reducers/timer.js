const INITIAL_STATE = {
  time: 30,
  timerID: '',
};

const timerReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default timerReducer;
